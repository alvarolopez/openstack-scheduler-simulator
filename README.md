Sinovass: Simple Nova's Scheduler Simulator
===========================================

1. Install a virtualenv and install nova

```
    virtualenv --system-site-packages .venv
    source .venv/bin/activate
    pip install -e /path/to/nova
```

2. Run the simulator `python simulate.py`
